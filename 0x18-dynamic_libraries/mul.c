#include <stdio.h>
#include <stdlib.h>

/**
 * mul - multiply two integers
 * @a: first int
 * @b: second int
 * Return: multiplication of the two number
 */
int mul(int a, int b)
{
	return (a * b);
}
